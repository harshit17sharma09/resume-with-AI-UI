export const metadata = {
    title: "Resume Chatbot",
    description: "Upload your resume and chat with our AI chatbot",
  };
  
  export default function MetadataLayout({ children }) {
    return (
      <html lang="en">
        <body>{children}</body>
      </html>
    );
  }
  